/**
 * defines what a column is like
 * 
 * @interface IColumns
 */
interface IColumns {
    /**
     * Name of the column
     * 
     * @type {string}
     * @memberof IColumns
     */
    data: string;
}